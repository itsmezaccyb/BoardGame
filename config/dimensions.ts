/**
 * PIXELS_PER_INCH Configuration
 * 
 * This is the ONLY variable that needs to be changed when switching to a different screen.
 * 
 * To calibrate:
 * 1. Display a 1 inch × 1 inch square on your screen
 * 2. Measure the actual physical size with a ruler
 * 3. Adjust this value until the displayed size matches 1 inch
 * 
 * Default value is optimized for large TV displays (55"+)
 */
export const PIXELS_PER_INCH = 40.097;

